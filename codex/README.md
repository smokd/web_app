# Project Handoff Memory

This directory is the canonical, repository-local handoff record for Codex and ChatGPT.

Start each session with `PROJECT_STATE.md`, then the relevant module file in `modules/`, and verify statements against source. Use `DONE`, `IMPLEMENTED`, `IN PROGRESS`, `BLOCKED`, and `TODO` precisely.

Update `PROJECT_STATE.md` after meaningful discoveries or changes; update the related reference files at the same time.
